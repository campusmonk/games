"use strict";
/**
 * Campusmonk — Accenture Technical Assessment Practice Quiz
 * TypeScript conversion of the original inline <script> logic.
 *
 * Compile with: tsc app.ts --target ES2017 --lib DOM,ES2017 --module none
 * (see tsconfig.json in this folder) and include the emitted app.js
 * in the page in place of the original inline script.
 */
const BANK = [{ "id": "office", "name": "Common Applications & MS Office", "short": "MS Office", "questions": [{ "q": "A finance analyst receives a workbook containing 18,000 transaction rows. The summary sheet must show revenue by region, while the source data must remain untouched and managers must be able to refresh the summary when new rows are appended. Which approach is the most appropriate?", "options": ["Create a PivotTable from the transaction range and refresh it after the source expands", "Use Find and Replace to convert every region into a separate worksheet", "Sort the source rows and manually add regional totals below them", "Copy the visible rows into PowerPoint and calculate totals there"], "answer": 0, "explanation": "A PivotTable is designed for grouped summaries and can be refreshed from its source without manually rebuilding the report.", "code": null, "tag": "Accenture-style Hard Practice", "difficulty": "Hard" }, { "q": "An Excel model calculates commission using `=C2*$F$1`, where C2 is the employee's sales and F1 contains the commission rate. The formula will be filled down 500 rows, but the rate must always remain in F1. Which modification preserves the intended dependency?", "options": ["=C2*F1", "=$C$2*F1", "=C2*$F$1", "=$C2*$F1"], "answer": 2, "explanation": "The absolute reference $F$1 fixes both the column and row while C2 remains relative and changes by row.", "code": null, "tag": "Accenture-style Hard Practice", "difficulty": "Hard" }, { "q": "A manager wants a worksheet to display only employees whose quarterly sales are above ₹8,00,000, without deleting or rearranging any underlying records. The filter should be reversible and should work with other column filters simultaneously. Which Excel feature best matches the requirement?", "options": ["AutoFilter", "Conditional Formatting", "Data Validation", "Goal Seek"], "answer": 0, "explanation": "AutoFilter hides rows that do not satisfy the selected criteria while preserving the source data.", "code": null, "tag": "Accenture-style Hard Practice", "difficulty": "Hard" }, { "q": "A large workbook contains formulas referencing cells on three different sheets. A developer needs to inspect which cells feed a selected formula before changing it, because a mistaken edit could affect several downstream calculations. Which Excel auditing feature is most useful?", "options": ["Remove Duplicates", "Text to Columns", "Freeze Panes", "Trace Dependents"], "answer": 3, "explanation": "Trace Dependents shows cells whose formulas depend on the selected cell, helping assess downstream impact.", "code": null, "tag": "Accenture-style Hard Practice", "difficulty": "Hard" }, { "q": "A client-facing Word report contains a 40-page table of contents. Headings are already formatted using consistent heading styles, and the report will be revised several times. The team wants page numbers and heading entries to update automatically after edits. What is the most reliable workflow?", "options": ["Use WordArt for each heading and group the objects", "Convert the document to plain text before every revision", "Use Heading styles and insert/update an automatic Table of Contents", "Insert a manual list of headings and type page numbers"], "answer": 2, "explanation": "Word's automatic Table of Contents is generated from heading styles and can be updated after document changes.", "code": null, "tag": "Accenture-style Hard Practice", "difficulty": "Hard" }, { "q": "A PowerPoint deck is being prepared for an executive review. The company logo, footer, slide number, and title placement must remain consistent across 60 slides, including slides that will be created later. Which design mechanism should the team use rather than editing each slide individually?", "options": ["Animation Pane", "Slide Master", "Reading View", "Presenter View"], "answer": 1, "explanation": "The Slide Master controls common layout and design elements across slides using that master.", "code": null, "tag": "Accenture-style Hard Practice", "difficulty": "Hard" }, { "q": "An analyst needs to count only cells in D2:D500 whose values are greater than 1000. Blank cells and text labels should not be counted, and the result must update automatically as values change. Which formula is the most direct solution?", "options": ["=COUNTA(D2:D500)", "=COUNT(D2:D500,\">1000\")", "=COUNTIF(D2:D500,\">1000\")", "=SUMIF(D2:D500,\">1000\")"], "answer": 2, "explanation": "COUNTIF applies a single criterion to a range and counts cells whose values satisfy it.", "code": null, "tag": "Accenture-style Hard Practice", "difficulty": "Hard" }, { "q": "A worksheet contains customer IDs in column A and order values in column B. The same customer can appear many times. You need a list containing each customer exactly once, while retaining the original data for audit purposes. Which operation is most appropriate on a copy of the data?", "options": ["Scenario Manager", "Protect Sheet", "Goal Seek", "Remove Duplicates"], "answer": 3, "explanation": "Remove Duplicates can produce a unique list while the original source remains available if performed on a copied range.", "code": null, "tag": "Accenture-style Hard Practice", "difficulty": "Hard" }, { "q": "A finance workbook uses a named range `TaxRate` for a value stored on a settings sheet. Several formulas across multiple sheets reference the same rate. The team wants formulas to remain readable and resilient if the settings cell is moved. What is the main advantage of using the named range?", "options": ["It provides a meaningful reference independent of the physical cell address", "It converts all formulas into VBA macros", "It prevents the value from being changed by anyone", "It forces every formula to become an array formula"], "answer": 0, "explanation": "A defined name gives formulas a semantic reference and can continue pointing to the intended cell or range if its location changes.", "code": null, "tag": "Accenture-style Hard Practice", "difficulty": "Hard" }, { "q": "A Word document is edited by four reviewers. The legal team requires every insertion and deletion to remain visible so that the author can accept or reject changes individually. Simply exchanging a final copy is not sufficient. Which feature should be enabled?", "options": ["Track Changes", "Word Count", "Mail Merge", "Page Color"], "answer": 0, "explanation": "Track Changes records insertions, deletions and other revisions so they can later be reviewed and accepted or rejected.", "code": null, "tag": "Accenture-style Hard Practice", "difficulty": "Hard" }, { "q": "A PowerPoint presentation contains a chart that is linked to an Excel workbook. Before presenting, the analyst changes the Excel figures and expects the chart to reflect the updated values. Which behavior is the key benefit of maintaining a linked chart rather than pasting a static picture?", "options": ["The chart automatically writes formulas into every Excel cell", "The chart becomes independent of all source data", "The chart can update from the source workbook when the link is available", "The chart can only be edited from Presenter View"], "answer": 2, "explanation": "A linked chart can retrieve updated source data, unlike a static pasted image.", "code": null, "tag": "Accenture-style Hard Practice", "difficulty": "Hard" }, { "q": "An Excel dashboard must let a manager choose a department from a controlled list such as HR, Finance, Sales, or IT. Users should not accidentally type arbitrary department names because downstream formulas depend on exact matches. Which feature is designed for this input constraint?", "options": ["Consolidate", "Data Validation with a list", "Conditional Formatting", "Flash Fill"], "answer": 1, "explanation": "Data Validation can restrict a cell to values from a specified list, preventing inconsistent manual entries.", "code": null, "tag": "Accenture-style Hard Practice", "difficulty": "Hard" }, { "q": "A workbook is shared with a team. Users should be able to enter values into input cells but must not accidentally overwrite formulas in calculation cells. The protection must be reversible by an authorized user. What should be done first?", "options": ["Hide the workbook window", "Unlock the intended input cells, then protect the worksheet", "Convert every formula to text", "Delete the calculation formulas and replace them with values"], "answer": 1, "explanation": "Cells intended for input can be unlocked before sheet protection is enabled, leaving protected cells guarded against edits.", "code": null, "tag": "Accenture-style Hard Practice", "difficulty": "Hard" }, { "q": "A PowerPoint deck contains several objects that must move together as a single visual unit, while preserving their relative positions. The objects may still need to be edited individually later. Which operation is most appropriate?", "options": ["Flatten the slide into a bitmap", "Group the objects", "Convert the slide to notes", "Apply a transition to every object"], "answer": 1, "explanation": "Grouping lets multiple objects be manipulated together while retaining the individual objects within the group.", "code": null, "tag": "Accenture-style Hard Practice", "difficulty": "Hard" }, { "q": "An Excel report must return the largest sales value among rows belonging to a selected region, not simply the largest value in the entire column. The solution should react when the region selector changes. Which formula family is best suited to the requirement?", "options": ["A simple MAX over the whole sales column", "COUNTA over the region column", "ROUND over the selected region text", "A conditional aggregate such as MAXIFS"], "answer": 3, "explanation": "MAXIFS evaluates a maximum while applying one or more criteria, making it suitable for a selected region.", "code": null, "tag": "Accenture-style Hard Practice", "difficulty": "Hard" }] }, { "id": "sql", "name": "SQL & Query Logic", "short": "SQL", "questions": [{ "q": "An order table contains millions of rows. A report must return customers whose completed orders have a combined value above ₹50,000, but cancelled orders must be ignored before the grouping occurs. Which query structure correctly separates row-level filtering from group-level filtering?", "options": ["HAVING status='Completed' before GROUP BY, followed by WHERE SUM(amount)>50000", "ORDER BY amount before WHERE status='Completed' and GROUP BY customer_id", "GROUP BY first, then WHERE SUM(amount)>50000 and finally HAVING status='Completed'", "WHERE status='Completed' before GROUP BY, followed by HAVING SUM(amount)>50000"], "answer": 3, "explanation": "WHERE removes unwanted rows before aggregation; HAVING then filters groups using the aggregate result.", "code": null, "tag": "Accenture-style Hard Practice", "difficulty": "Hard" }, { "q": "Consider Customers(id) and Orders(customer_id, amount). The business report must include every customer, including customers who have never placed an order, while showing NULL or an equivalent empty result for missing order data. Which join preserves that requirement?", "options": ["INNER JOIN", "RIGHT JOIN from Orders to Customers", "CROSS JOIN", "LEFT JOIN from Customers to Orders"], "answer": 3, "explanation": "A LEFT JOIN keeps every row from the left table, even when no matching row exists on the right.", "code": null, "tag": "Accenture-style Hard Practice", "difficulty": "Hard" }, { "q": "A backend service executes the following query against an Orders table. The developer expects one row per customer but sees multiple rows for customers with multiple orders. The requirement is to return each customer's latest order only. Which SQL technique is most reliable when the database supports window functions?", "options": ["DISTINCT customer_id, order_date without defining which order is latest", "ORDER BY order_date DESC alone", "ROW_NUMBER() OVER (PARTITION BY customer_id ORDER BY order_date DESC) and keep row_number=1", "COUNT(*) GROUP BY customer_id without considering order_date"], "answer": 2, "explanation": "ROW_NUMBER partitions rows by customer and orders each partition so the first row represents the latest order.", "code": null, "tag": "Accenture-style Hard Practice", "difficulty": "Hard" }, { "q": "A production query uses `WHERE salary > (SELECT AVG(salary) FROM Employee)` to return employees earning above the company average. The requirement changes to above the average salary within each department. What must change conceptually?", "options": ["The average must be calculated once globally and compared to every row", "The average must be correlated or computed per department before comparing each employee", "GROUP BY can be omitted because departments are irrelevant", "The salary column must be converted to text"], "answer": 1, "explanation": "The comparison target must be department-specific, which requires a correlated or grouped department-level average.", "code": null, "tag": "Accenture-style Hard Practice", "difficulty": "Hard" }, { "q": "A table stores `employee_id`, `department_id`, and `salary`. A report groups by department and wants departments whose average salary exceeds ₹80,000. Which clause belongs after the grouping to express that condition?", "options": ["ON AVG(salary) > 80000", "HAVING AVG(salary) > 80000", "WHERE AVG(salary) > 80000", "ORDER BY AVG(salary) > 80000"], "answer": 1, "explanation": "HAVING filters groups after aggregate calculations such as AVG are evaluated.", "code": null, "tag": "Accenture-style Hard Practice", "difficulty": "Hard" }, { "q": "A query joins Customers to Orders and then applies `WHERE Orders.status = 'PAID'`. The team expected customers with no orders to remain visible, but they disappear. Why can this happen, and what is a common correction when unmatched customers must remain?", "options": ["WHERE always converts every JOIN into a CROSS JOIN", "The WHERE condition can eliminate NULL-extended rows from the outer join; move the condition into the JOIN predicate or handle NULL explicitly", "The primary key is automatically removed by WHERE", "A WHERE clause cannot reference columns from a joined table"], "answer": 1, "explanation": "Rows produced by the outer join with no match have NULL order columns; filtering those rows in WHERE removes them. Putting the status predicate in ON preserves unmatched customers.", "code": null, "tag": "Accenture-style Hard Practice", "difficulty": "Hard" }, { "q": "A developer needs to change every product price in a category by 5%, but only rows satisfying the category and active-status conditions should be modified. Which statement is the appropriate operation, assuming the transaction has already been reviewed?", "options": ["ALTER TABLE with a WHERE clause", "GROUP BY with SET", "UPDATE with a restrictive WHERE clause", "SELECT with a calculated alias"], "answer": 2, "explanation": "UPDATE changes existing rows, and the WHERE clause limits the modification to the intended records.", "code": null, "tag": "Accenture-style Hard Practice", "difficulty": "Hard" }, { "q": "A table contains nullable `discount` values. A report uses `SUM(discount)` and the analyst assumes a NULL discount contributes zero to the total. Which statement best describes the standard aggregate behavior?", "options": ["SUM generally ignores NULL values rather than treating each NULL as a numeric zero row", "SUM fails whenever one row contains NULL", "SUM counts NULL as the number of rows in the table", "SUM always converts NULL to -1"], "answer": 0, "explanation": "Most SQL aggregate functions such as SUM ignore NULL inputs; they do not add NULL as a numeric value.", "code": null, "tag": "Accenture-style Hard Practice", "difficulty": "Hard" }, { "q": "A login query is written by concatenating raw user input into SQL text. Apart from correctness problems, an attacker may supply crafted input that changes the intended predicate. Which mitigation is the strongest standard application-level approach?", "options": ["Use parameterized/prepared statements", "Escape only the final semicolon character", "Hide the database port from the browser while keeping string concatenation", "Convert the username to uppercase before concatenation"], "answer": 0, "explanation": "Parameterized queries keep user data separate from SQL structure and are the standard defense against injection.", "code": null, "tag": "Accenture-style Hard Practice", "difficulty": "Hard" }, { "q": "An index exists on `Orders(customer_id, order_date)`. A query frequently filters by customer_id and then sorts or ranges by order_date. Why can this composite index be more useful than two unrelated single-column indexes for that access pattern?", "options": ["It guarantees that no transaction can update an order", "It automatically replaces the need for a primary key", "Its ordered key structure can support the leading customer_id predicate and then efficiently narrow or order by order_date", "Composite indexes always use zero storage"], "answer": 2, "explanation": "The leftmost prefix of the composite index aligns with customer_id and can then exploit the ordered order_date portion for suitable predicates or ordering.", "code": null, "tag": "Accenture-style Hard Practice", "difficulty": "Hard" }, { "q": "A transaction inserts a payment row and then fails before updating the corresponding order status. The application requires both changes to appear together or neither to persist. Which database mechanism directly addresses this all-or-nothing requirement?", "options": ["A transaction with COMMIT/ROLLBACK semantics", "A VIEW without a transaction", "An index on payment_id", "A SELECT DISTINCT clause"], "answer": 0, "explanation": "A transaction can commit both changes atomically or roll them back together after a failure.", "code": null, "tag": "Accenture-style Hard Practice", "difficulty": "Hard" }, { "q": "Two queries need the same result set but one uses `UNION` and the other uses `UNION ALL`. The source queries intentionally contain duplicate rows and the business rule says duplicates must be retained because each row represents a separate event. Which operator is correct?", "options": ["UNION ALL, because it concatenates results without duplicate elimination", "EXCEPT, because it retains the first query's duplicates", "INTERSECT, because it retains duplicates", "UNION, because it preserves all rows"], "answer": 0, "explanation": "UNION ALL retains duplicate rows, whereas UNION removes duplicates from the combined result.", "code": null, "tag": "Accenture-style Hard Practice", "difficulty": "Hard" }, { "q": "A query must find customers who have placed at least one order, but the report does not need order columns and should avoid accidental row multiplication when a customer has many orders. Which construct expresses the existence test most directly?", "options": ["A scalar arithmetic expression on customer_id", "EXISTS with a correlated subquery", "CROSS JOIN followed by DISTINCT", "ORDER BY with LIMIT 1"], "answer": 1, "explanation": "EXISTS checks whether at least one matching row exists without returning or multiplying the matching rows.", "code": null, "tag": "Accenture-style Hard Practice", "difficulty": "Hard" }, { "q": "A SQL query uses `WHERE amount = NULL` to find rows with missing amounts and returns no matches. What is the correct explanation?", "options": ["Equality to NULL works only after ORDER BY", "NULL is stored as the string 'NULL' and needs LIKE", "NULL represents an unknown/missing value and must be tested with IS NULL rather than ordinary equality", "NULL can only be tested using HAVING"], "answer": 2, "explanation": "Comparisons with NULL use three-valued logic; IS NULL and IS NOT NULL are the standard predicates.", "code": null, "tag": "Accenture-style Hard Practice", "difficulty": "Hard" }, { "q": "A reporting query joins three tables and unexpectedly produces far more rows than any source table contains. Investigation shows two joins match multiple rows on both sides. What is the most likely cause?", "options": ["GROUP BY automatically duplicates every row", "The database has duplicated primary keys by design", "ORDER BY creates additional physical rows", "A many-to-many join multiplication caused by non-unique join relationships"], "answer": 3, "explanation": "When both sides have multiple matching rows, combinations can multiply the result cardinality unless the relationship or join condition is constrained.", "code": null, "tag": "Accenture-style Hard Practice", "difficulty": "Hard" }] }, { "id": "pseudo", "name": "Pseudo Code & Programming Logic", "short": "Pseudo Code", "questions": [{ "q": "An assessment contains the following pseudocode. Assume integer division is used and MOD returns the remainder. Without executing it, determine the final value printed after both loops finish.", "options": ["26", "28", "24", "30"], "answer": 1, "explanation": "The inner accumulation contributes the required remainder-adjusted values across the three outer iterations, producing 28.", "code": "SET total = 0\nFOR i = 1 TO 3\n    FOR j = 2 TO 5\n        IF (i + j) MOD 2 = 0 THEN\n            total = total + j\n        ELSE\n            total = total + 1\n        END IF\n    END FOR\nEND FOR\nPRINT total", "tag": "Accenture-style Hard Practice", "difficulty": "Hard" }, { "q": "A candidate traces this pseudocode in an interview. The important detail is that the function changes the same accumulator through recursive calls before returning. What is printed?", "options": ["13", "7", "9", "11"], "answer": 2, "explanation": "The recursive calls reduce n until the base case, then add the values while the call stack unwinds, giving 9.", "code": "FUNCTION f(n)\n    IF n <= 1 THEN\n        RETURN 1\n    END IF\n    RETURN n + f(n - 2)\nEND FUNCTION\nPRINT f(5)", "tag": "Accenture-style Hard Practice", "difficulty": "Hard" }, { "q": "A service receives an integer and repeatedly replaces it with integer division by 2 until it becomes zero. The developer claims the number of iterations is linear in the input value. Which complexity is the correct asymptotic bound?", "options": ["O(1)", "O(n log n)", "O(log n)", "O(n²)"], "answer": 2, "explanation": "Repeatedly halving n reduces the value exponentially, so the number of iterations grows logarithmically.", "code": null, "tag": "Accenture-style Hard Practice", "difficulty": "Hard" }, { "q": "Trace the following logic carefully. The condition uses short-circuit AND, so the second expression is evaluated only when the first is true. What value is printed?", "options": ["1", "3", "0", "2"], "answer": 1, "explanation": "Only iterations satisfying the first condition evaluate the second; the counter is incremented on the matching branch, producing 3.", "code": "count = 0\nFOR i = 1 TO 6\n    IF i MOD 2 = 0 AND i MOD 3 = 0 THEN\n        count = count + 1\n    END IF\nEND FOR\nPRINT count", "tag": "Accenture-style Hard Practice", "difficulty": "Hard" }, { "q": "An array contains [4, 7, 2, 9, 5]. The pseudocode scans from left to right and updates `best` only when the current value is strictly greater than `best`. If `best` starts as the first element, what is printed?", "options": ["9", "4", "27", "5"], "answer": 0, "explanation": "The scan replaces best with 7, then 9; later values are smaller, so the final maximum is 9.", "code": "A = [4,7,2,9,5]\nbest = A[0]\nFOR i = 1 TO LENGTH(A)-1\n    IF A[i] > best THEN best = A[i]\nEND FOR\nPRINT best", "tag": "Accenture-style Hard Practice", "difficulty": "Hard" }, { "q": "A developer uses a stack to evaluate a sequence of operations. Pushes add to the top and pops remove the most recently pushed item. After the operations below, which sequence is printed by the final two pops?", "options": ["A C", "C A", "A B", "B C"], "answer": 3, "explanation": "The last-in-first-out behavior means C is removed first, then A, leaving B below them.", "code": "PUSH A\nPUSH B\nPOP -> x\nPUSH C\nPUSH x\nPOP -> p\nPOP -> q\nPRINT p, q", "tag": "Accenture-style Hard Practice", "difficulty": "Hard" }, { "q": "A loop uses two pointers, `left` and `right`, moving toward each other. Each iteration either increments left or decrements right, and neither pointer ever moves backward. Which complexity best describes the total number of iterations for an array of n elements?", "options": ["O(n)", "O(n²)", "O(n log n)", "O(log n)"], "answer": 0, "explanation": "Because the pointers collectively traverse at most n positions, the loop performs O(n) iterations.", "code": null, "tag": "Accenture-style Hard Practice", "difficulty": "Hard" }, { "q": "A function receives a sorted array and a target. It compares the target with the middle element and discards half the remaining search interval after each comparison. The interviewer asks why this is asymptotically faster than scanning every element. What is the key reason?", "options": ["Every comparison examines all remaining elements", "The target is always located at index zero", "The array becomes unsorted after every comparison", "Each comparison removes roughly half of the remaining candidates"], "answer": 3, "explanation": "Binary search reduces the search space by about half per step, giving logarithmic search time on a sorted array.", "code": null, "tag": "Accenture-style Hard Practice", "difficulty": "Hard" }, { "q": "Consider the recursive logic below. A candidate incorrectly counts the base case as zero because the recursive branch is the only visible addition. What is the actual return value for f(4)?", "options": ["7", "4", "6", "5"], "answer": 2, "explanation": "The base case returns 1, and the stack adds 4 + 2 + 1, so f(4) returns 7 if the recursive step subtracts two; the option matching the complete trace is 7.", "code": "FUNCTION f(n)\n    IF n <= 0 THEN RETURN 1\n    RETURN n + f(n-2)\nEND FUNCTION\nPRINT f(4)", "tag": "Accenture-style Hard Practice", "difficulty": "Hard" }, { "q": "A loop scans an array and maintains a running sum. If the sum becomes greater than 100, it breaks immediately rather than scanning the remaining elements. For worst-case complexity when no early break occurs, which bound should be reported?", "options": ["O(n)", "O(log n)", "O(1)", "O(n²)"], "answer": 0, "explanation": "In the worst case every element is inspected exactly once, so the running time is linear.", "code": null, "tag": "Accenture-style Hard Practice", "difficulty": "Hard" }, { "q": "A developer wants to reverse a string using only a stack. Characters are pushed in their original order and then popped until the stack is empty. What property guarantees the reversed order?", "options": ["Sorted ordering", "LIFO ordering", "Random ordering", "FIFO ordering"], "answer": 1, "explanation": "A stack is LIFO, so the last character pushed is the first character popped, reversing the sequence.", "code": null, "tag": "Accenture-style Hard Practice", "difficulty": "Hard" }, { "q": "A program checks whether an integer is a palindrome by converting it to a string and comparing symmetric characters. For an input with d digits, what is the additional time complexity of this check?", "options": ["O(log d)", "O(1)", "O(d²)", "O(d)"], "answer": 3, "explanation": "Each of the d characters may be inspected once, so the check is linear in the number of digits.", "code": null, "tag": "Accenture-style Hard Practice", "difficulty": "Hard" }, { "q": "A candidate is given a nested loop where the outer loop runs n times and the inner loop runs from 1 to the current outer index. The interviewer asks for the tight asymptotic bound rather than a loose upper bound. Which is correct?", "options": ["O(n²)", "O(n³)", "O(log n)", "O(n)"], "answer": 0, "explanation": "The total work is 1+2+...+n = n(n+1)/2, which is Θ(n²).", "code": null, "tag": "Accenture-style Hard Practice", "difficulty": "Hard" }, { "q": "An algorithm receives an array and uses a hash set to record every value seen so far. For each new value it checks membership before inserting it. Assuming average constant-time hashing, what is the expected time complexity over n values?", "options": ["O(log n)", "O(n)", "O(n²)", "O(1)"], "answer": 1, "explanation": "Each lookup and insertion is expected O(1), repeated n times, giving expected O(n) time.", "code": null, "tag": "Accenture-style Hard Practice", "difficulty": "Hard" }, { "q": "A program uses a queue for customer requests. Three requests A, B and C arrive in that order. The program dequeues one request, enqueues D, then dequeues two more. Which request is processed last among A, B, C and D?", "options": ["A", "B", "D", "C"], "answer": 2, "explanation": "A is removed first, then B; the queue contains C and D, so C is removed before D.", "code": null, "tag": "Accenture-style Hard Practice", "difficulty": "Hard" }] }, { "id": "net", "name": "Networking, Security & Cloud", "short": "Networking", "questions": [{ "q": "An enterprise portal is reachable by IP address, but users should access it as `portal.corp.example`. The mapping may change when the service moves to a new server, and clients should not need to know the new IP. Which infrastructure service provides this abstraction?", "options": ["NTP", "DNS", "ARP", "DHCP"], "answer": 1, "explanation": "DNS maps human-readable hostnames to IP addresses and allows the mapping to change independently of clients.", "code": null, "tag": "Accenture-style Hard Practice", "difficulty": "Hard" }, { "q": "A branch has 120 employee laptops that should receive IP addresses, subnet masks, gateways and DNS servers automatically. The network team wants leases to expire and addresses to be reused when devices leave. Which service is intended for this lifecycle?", "options": ["DHCP", "SNMP", "DNS", "TLS"], "answer": 0, "explanation": "DHCP dynamically supplies network configuration and manages address leases.", "code": null, "tag": "Accenture-style Hard Practice", "difficulty": "Hard" }, { "q": "A web application sends sensitive credentials over an untrusted network. The team wants confidentiality, integrity and server authentication while retaining normal HTTP semantics for the application. Which protocol arrangement is most appropriate?", "options": ["HTTP over TLS (HTTPS)", "Telnet over TCP", "FTP without encryption", "DNS over UDP only"], "answer": 0, "explanation": "HTTPS uses HTTP over TLS, providing encrypted transport plus authentication and integrity protections.", "code": null, "tag": "Accenture-style Hard Practice", "difficulty": "Hard" }, { "q": "A subnet is configured as `192.168.10.0/26`. The network team asks how many usable host addresses are available for ordinary IPv4 hosts, excluding the network and broadcast addresses. What is the correct count?", "options": ["64", "62", "30", "126"], "answer": 1, "explanation": "A /26 leaves 6 host bits, giving 64 addresses; excluding network and broadcast leaves 62 usable host addresses.", "code": null, "tag": "Accenture-style Hard Practice", "difficulty": "Hard" }, { "q": "A load-balanced web service uses TCP connections from clients to a public endpoint. The load balancer must distribute connections while preserving a reliable byte stream between endpoints. Which statement best describes TCP compared with UDP?", "options": ["TCP is limited to local networks whereas UDP works globally", "UDP always encrypts traffic while TCP never can", "TCP provides connection-oriented reliable delivery; UDP does not provide the same built-in reliability guarantees", "TCP cannot detect packet loss"], "answer": 2, "explanation": "TCP provides sequencing, acknowledgements and retransmission mechanisms; UDP is connectionless and leaves reliability to higher layers.", "code": null, "tag": "Accenture-style Hard Practice", "difficulty": "Hard" }, { "q": "An administrator notices that a private cloud subnet uses RFC1918 addresses, while users on the internet see a single public address for outbound traffic. Which mechanism commonly enables many private hosts to share public IPv4 space?", "options": ["DHCP relay", "NAT/PAT", "ARP poisoning", "VLAN tagging"], "answer": 1, "explanation": "NAT, often with port address translation, maps private addresses and ports to a public address space.", "code": null, "tag": "Accenture-style Hard Practice", "difficulty": "Hard" }, { "q": "A security review finds that a cloud storage bucket contains confidential customer exports. The requirement is that even if an attacker obtains a copy of the underlying storage object, the data should not be readable without the relevant cryptographic protection. Which control directly addresses this?", "options": ["Encryption at rest", "Changing the load-balancer algorithm", "Disabling DNS", "Increasing CPU allocation"], "answer": 0, "explanation": "Encryption at rest protects stored data so possession of the raw storage representation does not directly reveal plaintext.", "code": null, "tag": "Accenture-style Hard Practice", "difficulty": "Hard" }, { "q": "A company wants engineers to access only the cloud resources required for their job roles. A developer should be able to deploy to a test account but must not delete production databases. Which security principle is being applied?", "options": ["Open access", "Least privilege", "Single sign-on without authorization", "Broadcast discovery"], "answer": 1, "explanation": "Least privilege grants only the permissions needed for a role, reducing the impact of compromised or misused credentials.", "code": null, "tag": "Accenture-style Hard Practice", "difficulty": "Hard" }, { "q": "A remote employee connects to a corporate service from an untrusted Wi-Fi network. The company wants the employee's device to establish an authenticated encrypted tunnel to the corporate network before internal resources are accessed. Which technology best matches this requirement?", "options": ["Plain HTTP proxy", "Hub", "VPN", "Unmanaged switch"], "answer": 2, "explanation": "A VPN establishes an encrypted tunnel and can authenticate the endpoint before allowing protected network access.", "code": null, "tag": "Accenture-style Hard Practice", "difficulty": "Hard" }, { "q": "A DNS resolver has cached an old IP address for a service. The authoritative record has already changed, but some clients continue receiving the previous address until the cache expires. Which DNS property primarily explains this behavior?", "options": ["MAC addresses force DNS records to remain permanent", "DHCP leases rewrite authoritative DNS data", "TTL controls how long cached records may be retained", "TCP window size determines DNS cache duration"], "answer": 2, "explanation": "The DNS TTL tells resolvers how long a cached record should be considered valid before it should be refreshed.", "code": null, "tag": "Accenture-style Hard Practice", "difficulty": "Hard" }, { "q": "A cloud workload receives a sudden flood of requests from thousands of sources. The team wants to absorb legitimate traffic spikes while filtering obviously malicious traffic before it reaches application instances. Which layered design is most appropriate?", "options": ["Disable logging to reduce overhead", "Expose every database port directly to the internet", "Put all traffic through a single unprotected VM", "Edge/WAF or DDoS protection in front of scalable application infrastructure"], "answer": 3, "explanation": "Edge protection and scalable front-end infrastructure can filter malicious patterns and absorb or distribute high traffic volumes.", "code": null, "tag": "Accenture-style Hard Practice", "difficulty": "Hard" }, { "q": "A network engineer wants to isolate finance devices from guest devices even though both use the same physical switch infrastructure. Devices in one logical segment should not receive layer-2 broadcasts from the other without routing. Which mechanism provides this segmentation?", "options": ["TLS certificates", "DNS aliases", "NAT alone", "VLANs"], "answer": 3, "explanation": "VLANs create separate layer-2 broadcast domains on shared switching infrastructure.", "code": null, "tag": "Accenture-style Hard Practice", "difficulty": "Hard" }, { "q": "A server has IP address 10.0.2.15 and subnet mask 255.255.255.0. A packet is destined for 10.0.2.88. Before sending it to the default gateway, what should the host determine?", "options": ["The destination must always be sent to the default gateway", "DNS must translate 10.0.2.88 into another IP", "The destination is on the same local subnet, so it can resolve the destination's MAC address and send directly", "The host must establish a TLS session with the router"], "answer": 2, "explanation": "With a /24 mask, both addresses belong to 10.0.2.0/24, so the host treats the destination as local and can resolve its MAC using ARP.", "code": null, "tag": "Accenture-style Hard Practice", "difficulty": "Hard" }, { "q": "An incident responder sees repeated failed logins followed by a successful login from an unusual geography. The team wants stronger evidence that a second factor is required even if the password is compromised. Which control most directly reduces the risk?", "options": ["Using a longer session timeout", "Increasing screen brightness", "Disabling audit logs", "Multi-factor authentication"], "answer": 3, "explanation": "MFA requires an additional authentication factor, so possession of the password alone is insufficient.", "code": null, "tag": "Accenture-style Hard Practice", "difficulty": "Hard" }, { "q": "A cloud application uses temporary credentials for a workload rather than embedding a long-lived access key in source code. The credentials are scoped to a role and expire automatically. What security advantage is being emphasized?", "options": ["Reduced exposure from long-lived secrets and tighter role-based permissions", "Permanent access even after the workload is deleted", "Removal of the need for authorization policies", "Guaranteed prevention of every application vulnerability"], "answer": 0, "explanation": "Short-lived role credentials reduce the damage window of credential leakage and can be scoped to the workload's required permissions.", "code": null, "tag": "Accenture-style Hard Practice", "difficulty": "Hard" }] }, { "id": "html", "name": "HTML & CSS", "short": "HTML/CSS", "questions": [{ "q": "A production checkout page must remain semantically meaningful and accessible. The designer wants a clickable control that submits the current form and can be activated using keyboard interaction. Which native element is the best starting point?", "options": ["<span>", "<button type=\"submit\">", "<p>", "<div>"], "answer": 1, "explanation": "A submit button is a semantic interactive control designed to submit a form and supports keyboard accessibility.", "code": null, "tag": "Accenture-style Hard Practice", "difficulty": "Hard" }, { "q": "A card contains a fixed-width content area with `width:300px; padding:20px; border:5px solid;` and uses the default content-box sizing model. Ignoring margins, what is the rendered outer width occupied by the element?", "options": ["340px", "300px", "350px", "330px"], "answer": 2, "explanation": "With content-box sizing, outer width is 300 + 20+20 padding + 5+5 border = 350px. Therefore the correct option is 350px.", "code": ".card{width:300px;padding:20px;border:5px solid;box-sizing:content-box;}", "tag": "Accenture-style Hard Practice", "difficulty": "Hard" }, { "q": "A responsive dashboard should use a two-column grid on wide screens but collapse to one column below 700px. The developer wants the same HTML structure and only CSS to change the layout based on viewport width. Which mechanism is intended for this requirement?", "options": ["JavaScript alert", "HTML comment", "DNS rule", "CSS media query"], "answer": 3, "explanation": "Media queries apply CSS rules conditionally based on characteristics such as viewport width.", "code": null, "tag": "Accenture-style Hard Practice", "difficulty": "Hard" }, { "q": "A team wants spacing between grid items without adding margin to each child and without changing the size of the grid container itself. Which CSS property is specifically designed to control the gap between grid or flex items?", "options": ["opacity", "z-index", "float", "gap"], "answer": 3, "explanation": "The gap property defines spacing between flex or grid items without requiring individual child margins.", "code": null, "tag": "Accenture-style Hard Practice", "difficulty": "Hard" }, { "q": "An image is decorative and conveys no information. The accessibility review asks what should be supplied so assistive technologies do not announce meaningless content. Which markup is appropriate?", "options": ["alt=\"\" on the decorative image", "A hidden H1 inside the image", "A title attribute containing the full page text", "alt=\"decorative image\" on every decorative image"], "answer": 0, "explanation": "An empty alt attribute marks a decorative image so screen readers can skip it.", "code": null, "tag": "Accenture-style Hard Practice", "difficulty": "Hard" }, { "q": "A CSS rule `.nav a { color:red; }` and another rule `#main .nav a { color:blue; }` both match the same link. Assuming no inline style or !important rule, which color wins under normal specificity rules?", "options": ["Red, because the first rule appears first", "Neither rule applies because two selectors match", "Blue, because the second selector contains an ID and therefore has greater specificity", "The browser randomly chooses between them"], "answer": 2, "explanation": "The selector containing an ID has greater specificity than the class-only selector, so blue wins.", "code": null, "tag": "Accenture-style Hard Practice", "difficulty": "Hard" }, { "q": "A developer sets `display:flex` on a container and wants its child cards to share the available horizontal space equally, while allowing their content to shrink when needed. Which flex declaration on each card is the most direct pattern?", "options": ["position:absolute", "flex:1", "display:block only", "float:none only"], "answer": 1, "explanation": "flex:1 allows flex items to grow and share available space, subject to the flex container's constraints.", "code": null, "tag": "Accenture-style Hard Practice", "difficulty": "Hard" }, { "q": "A layout uses `position: absolute` for a badge that should stay anchored to a card rather than to the page. The card itself should establish the positioning context. Which property on the card is normally used?", "options": ["float: right", "overflow: scroll only", "position: relative", "display: inline"], "answer": 2, "explanation": "An absolutely positioned descendant is positioned relative to the nearest positioned ancestor; position: relative commonly establishes that containing block.", "code": null, "tag": "Accenture-style Hard Practice", "difficulty": "Hard" }, { "q": "A long product title must remain on one line in a compact card and should show an ellipsis when it exceeds the available width. Which combination is the standard single-line truncation pattern?", "options": ["white-space:normal; overflow:visible; text-overflow:none", "white-space:nowrap; overflow:hidden; text-overflow:ellipsis", "display:grid; text-overflow:clip only", "position:absolute; line-height:0"], "answer": 1, "explanation": "The three properties together prevent wrapping, clip overflow, and request an ellipsis for truncated inline text.", "code": null, "tag": "Accenture-style Hard Practice", "difficulty": "Hard" }, { "q": "A page contains a navigation region, a main article, and supporting information. The accessibility review wants landmarks that communicate the structure to assistive technologies without relying only on CSS classes. Which HTML5 approach is preferable?", "options": ["Use only bold text to identify regions", "Put all content inside a table for layout", "Use semantic elements such as nav, main and aside", "Wrap everything in div elements with random class names"], "answer": 2, "explanation": "Semantic landmarks convey document structure and are more meaningful to browsers and assistive technologies.", "code": null, "tag": "Accenture-style Hard Practice", "difficulty": "Hard" }, { "q": "A developer needs an element to span all columns of a CSS Grid container regardless of how many columns are currently defined. Which declaration on the grid child is most appropriate for a common fixed-column grid?", "options": ["column-span:all in every browser", "grid-column:1 / -1", "float:all", "grid-row:0 / 0"], "answer": 1, "explanation": "grid-column: 1 / -1 makes the item span from the first grid line through the last grid line.", "code": null, "tag": "Accenture-style Hard Practice", "difficulty": "Hard" }, { "q": "A modal overlay appears behind the page content even though its `z-index` is set to 9999. The element is positioned, but an ancestor creates a separate stacking context that changes how descendants participate in stacking. What should the developer investigate first?", "options": ["The HTML document title", "The user's keyboard layout", "The page's DNS records", "The ancestor's stacking-context properties and stacking order"], "answer": 3, "explanation": "z-index is not a global number; stacking contexts determine how positioned descendants are composited relative to other contexts.", "code": null, "tag": "Accenture-style Hard Practice", "difficulty": "Hard" }, { "q": "A form field is required for submission. The team wants the browser to perform the basic required-field validation without writing custom JavaScript. Which native HTML attribute expresses that constraint?", "options": ["required", "constraint=required", "validate=true", "must-fill"], "answer": 0, "explanation": "The required attribute activates built-in browser validation for a form control.", "code": null, "tag": "Accenture-style Hard Practice", "difficulty": "Hard" }, { "q": "A responsive image should not overflow its parent container when the viewport becomes narrow. The image should preserve its intrinsic aspect ratio while shrinking to fit the available width. Which CSS rule is the common baseline?", "options": ["max-width:100%; height:auto;", "position:fixed; left:0;", "overflow:visible on the image only", "width:2000px; height:2000px;"], "answer": 0, "explanation": "max-width:100% limits the image to its containing width while height:auto preserves its aspect ratio.", "code": null, "tag": "Accenture-style Hard Practice", "difficulty": "Hard" }, { "q": "A developer uses margin collapse unintentionally and observes that the top margin of a first child appears to move outside its parent. Which statement best explains the behavior in normal block formatting contexts?", "options": ["Adjacent vertical margins can collapse under specific block-flow conditions", "Flex items always collapse margins", "Horizontal padding is automatically removed", "Grid containers collapse every child margin into the viewport"], "answer": 0, "explanation": "Vertical margins can collapse between certain block-level boxes, which can make a child's top margin appear outside its parent unless a new formatting context or other boundary prevents it.", "code": null, "tag": "Accenture-style Hard Practice", "difficulty": "Hard" }] }, { "id": "js", "name": "JavaScript & Web Logic", "short": "JavaScript", "questions": [{ "q": "A frontend function receives `input` from an API and should execute a branch only when the value is exactly the number 10, not the string `\"10\"`. Which comparison expresses that requirement most safely?", "options": ["input === 10", "input = 10", "String(input) = 10", "input == 10"], "answer": 0, "explanation": "Strict equality checks both value and type, so the string \"10\" does not equal the number 10.", "code": null, "tag": "Accenture-style Hard Practice", "difficulty": "Hard" }, { "q": "Consider the following JavaScript. The interviewer asks for the final value of `x` after the block executes. What should be selected?", "options": ["2", "3", "1", "ReferenceError"], "answer": 3, "explanation": "let is block scoped, so the inner x is separate from the outer x; the outer value remains 1 unless changed after the block.", "code": "let x = 1;\nif (true) {\n  let x = 3;\n  x++;\n}\nconsole.log(x);", "tag": "Accenture-style Hard Practice", "difficulty": "Hard" }, { "q": "A function accepts an optional configuration object. The developer writes `function build(opts = {})` and calls `build(undefined)`. Which behavior is guaranteed by default parameters?", "options": ["opts receives null automatically", "opts receives an empty object because the argument is undefined", "The function throws because defaults only work for omitted commas", "opts becomes the string '{}'"], "answer": 1, "explanation": "A default parameter is used when the corresponding argument is undefined, including when no argument is supplied.", "code": null, "tag": "Accenture-style Hard Practice", "difficulty": "Hard" }, { "q": "A web page schedules a callback with `setTimeout(..., 0)` and immediately performs a synchronous loop that takes noticeable time. Which callback-ordering statement is correct?", "options": ["The timeout callback always runs before the loop", "The timeout callback cannot interrupt the current synchronous call stack and runs after the stack is cleared", "The browser converts the loop into a promise automatically", "setTimeout(0) makes the callback synchronous"], "answer": 1, "explanation": "A timer callback is queued for later execution; JavaScript must finish the current synchronous stack before processing queued callbacks.", "code": null, "tag": "Accenture-style Hard Practice", "difficulty": "Hard" }, { "q": "A developer creates an object with a method that uses `this.name`. The method is passed as a standalone callback to another function, and `this` is no longer the original object in the same way. Which technique can preserve the lexical surrounding `this` when defining the callback?", "options": ["Use delete on the object", "Convert the name to CSS", "Use JSON.stringify on the callback", "Use an arrow function"], "answer": 3, "explanation": "Arrow functions do not create their own this binding; they capture this from the surrounding lexical scope.", "code": null, "tag": "Accenture-style Hard Practice", "difficulty": "Hard" }, { "q": "A React-like frontend state update stores an object and later mutates a nested property directly. The UI sometimes fails to recognize the change because the object reference remains unchanged. Which general state-management principle addresses this class of problem?", "options": ["Convert every object to a string permanently", "Always mutate nested objects before rendering", "Prefer immutable updates that create a new object reference for changed state", "Disable event handling after the first update"], "answer": 2, "explanation": "Immutable updates create new references, making change detection based on reference identity reliable in common UI architectures.", "code": null, "tag": "Accenture-style Hard Practice", "difficulty": "Hard" }, { "q": "A developer receives an array of user objects and wants a new array containing only users whose `active` property is true, without changing the original array. Which method is designed for this transformation?", "options": ["filter", "pop", "forEach only", "sort in place"], "answer": 0, "explanation": "filter returns a new array containing elements that satisfy the predicate and does not require mutating the original array.", "code": null, "tag": "Accenture-style Hard Practice", "difficulty": "Hard" }, { "q": "A function calls `fetch()` and the developer immediately reads the response body as though it were already available. Why is the direct synchronous assumption incorrect?", "options": ["Promises make all network calls synchronous", "fetch is asynchronous and returns a Promise representing future completion", "fetch always returns a number", "HTTP responses are stored in localStorage before fetch returns"], "answer": 1, "explanation": "fetch returns a Promise, so response processing must be scheduled through promise/async mechanisms.", "code": null, "tag": "Accenture-style Hard Practice", "difficulty": "Hard" }, { "q": "Consider `const a = {x:1}; a.x = 2;`. What does const actually prevent in this example?", "options": ["It converts the object to an immutable primitive", "It prevents reading x", "It freezes every nested property automatically", "It prevents reassignment of the variable binding, not mutation of the object's existing properties"], "answer": 3, "explanation": "const prevents assigning a new value to the variable binding; it does not automatically freeze the referenced object.", "code": null, "tag": "Accenture-style Hard Practice", "difficulty": "Hard" }, { "q": "A page registers the same click handler repeatedly each time a component is mounted, but fails to remove old handlers. After several mounts, one click triggers the logic multiple times. What is the most direct underlying problem?", "options": ["Event listeners accumulate because previous handlers were not cleaned up", "The DOM automatically deduplicates all callback functions", "HTTP caching creates extra click events", "CSS specificity causes JavaScript events to duplicate"], "answer": 0, "explanation": "Repeated registrations without cleanup can leave multiple active listeners attached to the same target.", "code": null, "tag": "Accenture-style Hard Practice", "difficulty": "Hard" }, { "q": "A developer needs to transform `[1,2,3,4]` into `[2,4,6,8]` without mutating the source array. Which method expresses the one-to-one transformation most clearly?", "options": ["splice", "reduce without a callback", "map", "filter"], "answer": 2, "explanation": "map creates a new array by applying a transformation to each source element.", "code": null, "tag": "Accenture-style Hard Practice", "difficulty": "Hard" }, { "q": "A promise chain has `.then(step1).then(step2).catch(handleError)`. If step1 rejects and step2 is never successfully produced, which part is intended to receive the rejection if no earlier catch handles it?", "options": ["The CSS parser", "The original function declaration", "The catch handler", "The final DOM element automatically"], "answer": 2, "explanation": "A catch later in the promise chain can handle rejections that propagate through preceding then callbacks.", "code": null, "tag": "Accenture-style Hard Practice", "difficulty": "Hard" }, { "q": "A JavaScript object is used as a key-value map, but keys may be objects themselves and the developer wants key identity rather than automatic string conversion. Which built-in collection is more appropriate?", "options": ["Array", "Map", "RegExp", "String"], "answer": 1, "explanation": "Map supports keys of arbitrary values, including objects, using key identity rather than stringifying object keys.", "code": null, "tag": "Accenture-style Hard Practice", "difficulty": "Hard" }, { "q": "A performance-sensitive page repeatedly queries the same DOM element inside a tight loop. The element does not change during the loop. Which simple optimization can reduce unnecessary DOM lookup work?", "options": ["Cache the element reference before the loop", "Rebuild the DOM on every iteration", "Call querySelector twice per iteration", "Convert the element to a CSS color"], "answer": 0, "explanation": "Caching a stable DOM reference avoids repeating the selector lookup in each iteration.", "code": null, "tag": "Accenture-style Hard Practice", "difficulty": "Hard" }, { "q": "A developer needs to execute an asynchronous operation and write sequential-looking code while still waiting without blocking the JavaScript thread. Which syntax is designed to express this clearly?", "options": ["goto/label only", "CSS @wait", "async/await", "synchronous XMLHttpRequest for every request"], "answer": 2, "explanation": "async functions with await provide readable promise-based control flow while the runtime remains event-driven rather than blocking the thread in the usual browser model.", "code": null, "tag": "Accenture-style Hard Practice", "difficulty": "Hard" }] }, { "id": "oop", "name": "OOP & Programming Concepts", "short": "OOP", "questions": [{ "q": "A banking system has `SavingsAccount` and `CurrentAccount` classes that share account-number validation and common balance behavior, but each provides its own withdrawal rules. Which design principle most directly allows the shared behavior to be reused while specialized classes override selected behavior?", "options": ["Compile-time commenting", "Global variables only", "Inheritance with method overriding", "Database normalization"], "answer": 2, "explanation": "Inheritance enables shared structure/behavior while overriding allows a subclass to provide specialized implementations.", "code": null, "tag": "Accenture-style Hard Practice", "difficulty": "Hard" }, { "q": "A payment service should expose `pay()` to callers but hide tokenization, retries, fraud checks and provider-specific details behind the service boundary. Which OOP concept is primarily being applied?", "options": ["Encapsulation/abstraction", "Multiple inheritance is mandatory", "Garbage collection only", "Operator precedence"], "answer": 0, "explanation": "The public operation hides internal implementation details, combining encapsulation with abstraction.", "code": null, "tag": "Accenture-style Hard Practice", "difficulty": "Hard" }, { "q": "A method named `calculateFee` has overloads for different parameter lists in a statically typed language. The compiler selects the appropriate method based on the arguments at compile time. Which form of polymorphism is this?", "options": ["Compile-time polymorphism through method overloading", "Encapsulation", "Aggregation", "Run-time polymorphism through overriding only"], "answer": 0, "explanation": "Overloading selects among methods with the same name but different signatures at compile time in languages that support it.", "code": null, "tag": "Accenture-style Hard Practice", "difficulty": "Hard" }, { "q": "A base class reference points to a derived-class object. The derived class overrides `calculate()`, and a call through the base reference executes the derived implementation at runtime. What OOP mechanism explains this behavior?", "options": ["Static variable hiding only", "Constructor chaining only", "Dynamic dispatch/runtime polymorphism", "Data normalization"], "answer": 2, "explanation": "When an overridden method is selected based on the actual object at runtime, dynamic dispatch provides runtime polymorphism.", "code": null, "tag": "Accenture-style Hard Practice", "difficulty": "Hard" }, { "q": "A `Car` object contains an `Engine` object, and the engine is conceptually a component of the car. The design team wants to express a whole-part relationship rather than saying Engine is a specialized Car. Which relationship is most appropriate?", "options": ["Method overloading", "Composition/has-a relationship", "Inheritance/is-a relationship", "Recursion"], "answer": 1, "explanation": "An engine is a part of a car, so composition models the has-a relationship.", "code": null, "tag": "Accenture-style Hard Practice", "difficulty": "Hard" }, { "q": "A class should guarantee that `balance` can never become negative except through a controlled overdraft operation. Direct external writes to the field must be prevented. Which implementation strategy best enforces the invariant?", "options": ["Keep the field public and rely on comments", "Store the balance in a CSS variable", "Remove all validation from the setter", "Make the field non-public and expose controlled methods that validate changes"], "answer": 3, "explanation": "Encapsulation protects the field and forces state changes through operations that can enforce business rules.", "code": null, "tag": "Accenture-style Hard Practice", "difficulty": "Hard" }, { "q": "An interface `Notifier` defines `send(message)`. EmailNotifier and SmsNotifier implement it differently. The order service depends only on Notifier rather than concrete provider classes. Which design benefit is strongest?", "options": ["Interfaces automatically store application state", "All implementations must share identical internal code", "The service can never have more than one notifier", "Low coupling through programming to an abstraction"], "answer": 3, "explanation": "Depending on an abstraction reduces coupling and makes alternative implementations easier to substitute or test.", "code": null, "tag": "Accenture-style Hard Practice", "difficulty": "Hard" }, { "q": "A subclass constructor must initialize fields declared by its parent class before adding its own state. In languages with explicit superclass construction, which concept ensures the parent initialization logic runs as required?", "options": ["Using a destructor before construction", "Using SQL JOIN", "Calling the superclass constructor", "Calling a static getter on the child"], "answer": 2, "explanation": "The superclass constructor initializes inherited/base-class state before or as part of derived-class construction according to the language rules.", "code": null, "tag": "Accenture-style Hard Practice", "difficulty": "Hard" }, { "q": "A developer creates a class whose only purpose is to represent a database connection, and every request creates a new instance even though one shared controlled instance is intended. Which design pattern is commonly associated with restricting a class to one managed instance?", "options": ["Singleton", "Adapter", "Strategy", "Observer"], "answer": 0, "explanation": "The Singleton pattern is intended to control creation so a class has a single shared instance, though it should be used judiciously.", "code": null, "tag": "Accenture-style Hard Practice", "difficulty": "Hard" }, { "q": "A system must allow a sorting algorithm to be selected at runtime without changing the code that processes orders. The available algorithms share a common operation but differ internally. Which design pattern best matches this requirement?", "options": ["Singleton", "Prototype only", "Builder", "Strategy"], "answer": 3, "explanation": "Strategy encapsulates interchangeable algorithms behind a common interface so the choice can vary independently of the caller.", "code": null, "tag": "Accenture-style Hard Practice", "difficulty": "Hard" }, { "q": "A class exposes a method that accepts a general `PaymentMethod` interface. Passing CardPayment, UpiPayment or WalletPayment should all work as long as they satisfy that interface. Which principle is demonstrated?", "options": ["Tight coupling to one concrete class", "Polymorphism through a common abstraction", "Multiple database transactions", "Global state"], "answer": 1, "explanation": "A common interface allows different concrete types to be used through the same abstraction.", "code": null, "tag": "Accenture-style Hard Practice", "difficulty": "Hard" }, { "q": "A developer copies an object by assigning `b = a` and expects a completely independent object. Later, changing b's field also affects a. What is the underlying issue in a reference-based object model?", "options": ["Inheritance forces every assignment to clone", "Both variables reference the same object rather than creating a separate object", "The garbage collector duplicates objects automatically", "The compiler converted the object into a primitive"], "answer": 1, "explanation": "Assignment of object references commonly copies the reference, so both variables can point to the same mutable object.", "code": null, "tag": "Accenture-style Hard Practice", "difficulty": "Hard" }, { "q": "A parent class declares a method as abstract because it knows the operation must exist but cannot provide a meaningful general implementation. What is the main purpose of such an abstraction?", "options": ["Force concrete subclasses to provide an implementation while defining a common contract", "Turn the method into a database query", "Make the method execute automatically in every subclass", "Prevent every subclass from using the method"], "answer": 0, "explanation": "Abstract methods define required behavior without supplying the complete implementation, leaving concrete subclasses responsible for it.", "code": null, "tag": "Accenture-style Hard Practice", "difficulty": "Hard" }, { "q": "A codebase has a class with dozens of unrelated responsibilities: database access, invoice formatting, email sending and payment calculation. Changes in one area frequently force tests for unrelated behavior. Which design principle is most directly being violated?", "options": ["CAP theorem", "Single Responsibility Principle", "FIFO principle", "Liskov Substitution Principle only"], "answer": 1, "explanation": "The Single Responsibility Principle favors a class having a focused reason to change rather than many unrelated responsibilities.", "code": null, "tag": "Accenture-style Hard Practice", "difficulty": "Hard" }, { "q": "A derived class changes the meaning of a base-class method so callers that expect the base contract can no longer rely on its promised behavior. Which SOLID principle is most directly threatened?", "options": ["Dependency Inversion Principle only", "Open/Closed Principle", "Liskov Substitution Principle", "Interface Segregation Principle only"], "answer": 2, "explanation": "LSP requires subtypes to remain substitutable for their base types without breaking expected behavioral contracts.", "code": null, "tag": "Accenture-style Hard Practice", "difficulty": "Hard" }] }, { "id": "dbms", "name": "DBMS & Transactions", "short": "DBMS", "questions": [{ "q": "A relational design stores a student's phone numbers as a comma-separated list in one column. Queries need to search individual phone numbers and enforce uniqueness. Which normalization concern is being violated most directly?", "options": ["Third Normal Form only", "First Normal Form, because a relation should use atomic values rather than repeating lists in one field", "No normal form applies to text columns", "Boyce-Codd Normal Form only"], "answer": 1, "explanation": "1NF requires atomic attributes rather than sets or repeating groups embedded in a single column.", "code": null, "tag": "Accenture-style Hard Practice", "difficulty": "Hard" }, { "q": "A table has a composite key `(student_id, course_id)` and a non-key attribute `student_name` depends only on `student_id`. Which normalization issue does this represent?", "options": ["Transitive dependency only, violating 3NF", "A deadlock", "A violation of atomicity only", "Partial dependency, violating 2NF"], "answer": 3, "explanation": "A non-key attribute depending on only part of a composite key is a partial dependency, which 2NF removes.", "code": null, "tag": "Accenture-style Hard Practice", "difficulty": "Hard" }, { "q": "A table has `employee_id -> department_id` and `department_id -> department_name`, while department_name is stored in the employee table. Which dependency pattern can cause update anomalies and is addressed by moving toward 3NF?", "options": ["Foreign-key indexing", "Multivalued dependency only", "Transitive dependency", "Partial dependency only"], "answer": 2, "explanation": "When a non-key attribute depends on another non-key attribute through a chain, the transitive dependency can violate 3NF.", "code": null, "tag": "Accenture-style Hard Practice", "difficulty": "Hard" }, { "q": "A payment transaction debits one account and credits another. If the credit fails after the debit, the system must leave both accounts exactly as they were before the transaction began. Which ACID property is most directly illustrated?", "options": ["Isolation", "Atomicity", "Durability", "Indexing"], "answer": 1, "explanation": "Atomicity ensures a transaction's operations are treated as one unit: either all required changes commit or none do.", "code": null, "tag": "Accenture-style Hard Practice", "difficulty": "Hard" }, { "q": "Two concurrent transactions update the same inventory row. Without adequate isolation, one transaction reads a value that another transaction later changes before the first transaction completes. Which phenomenon is commonly associated with such inconsistent intermediate reads?", "options": ["Non-repeatable read", "Normalization", "Syntax error", "Primary-key violation by definition"], "answer": 0, "explanation": "A non-repeatable read occurs when the same row is read twice and another committed transaction changes it between reads.", "code": null, "tag": "Accenture-style Hard Practice", "difficulty": "Hard" }, { "q": "A transaction reads a set of rows satisfying a condition. Another transaction inserts a new row satisfying that same condition before the first transaction repeats the query. What anomaly may appear under weaker isolation?", "options": ["Phantom read", "Stack overflow", "DNS poisoning", "Lost file descriptor"], "answer": 0, "explanation": "A phantom read occurs when a repeated range query sees newly inserted or removed rows matching the predicate.", "code": null, "tag": "Accenture-style Hard Practice", "difficulty": "Hard" }, { "q": "A database must ensure that once a committed payment transaction survives a power failure, its committed changes are not silently lost. Which ACID property is the primary guarantee?", "options": ["Atomicity", "Isolation", "Normalization", "Durability"], "answer": 3, "explanation": "Durability concerns the persistence of committed changes despite failures.", "code": null, "tag": "Accenture-style Hard Practice", "difficulty": "Hard" }, { "q": "Two transactions each hold a lock needed by the other and wait indefinitely unless one is aborted. What condition is being observed?", "options": ["Full table scan", "Dirty read", "Deadlock", "Referential integrity"], "answer": 2, "explanation": "A deadlock occurs when transactions wait on resources held by each other, creating a cycle of dependencies.", "code": null, "tag": "Accenture-style Hard Practice", "difficulty": "Hard" }, { "q": "A foreign key from Orders(customer_id) references Customers(id). The database rejects an order row whose customer_id does not exist. Which integrity rule is being enforced?", "options": ["File compression", "Referential integrity", "Domain independence", "View serialization"], "answer": 1, "explanation": "A foreign key maintains a valid relationship between referencing rows and referenced parent rows.", "code": null, "tag": "Accenture-style Hard Practice", "difficulty": "Hard" }, { "q": "A database designer wants a query to retrieve a customer by an exact unique email quickly, but the email column is not the primary key. Which structure is typically appropriate?", "options": ["A trigger that runs only after SELECT", "A foreign key to the email string itself", "A view with no supporting index", "An index on email, ideally with a uniqueness constraint when business rules require it"], "answer": 3, "explanation": "An index can accelerate lookup by email, and a unique constraint/index can enforce uniqueness if required.", "code": null, "tag": "Accenture-style Hard Practice", "difficulty": "Hard" }, { "q": "A transaction updates multiple rows and then issues ROLLBACK before COMMIT. What is the expected transactional effect for those uncommitted changes?", "options": ["They are converted into SELECT results", "They are automatically replicated to every client", "They are undone according to the database's transaction semantics", "They become permanently durable immediately"], "answer": 2, "explanation": "ROLLBACK discards the transaction's uncommitted changes, subject to the database's transaction behavior.", "code": null, "tag": "Accenture-style Hard Practice", "difficulty": "Hard" }, { "q": "A view is created as a saved query over employee and department tables. The application should query the view without needing to know the underlying join details. What is the main abstraction provided by the view?", "options": ["A mechanism that disables transactions", "An automatic replacement for all indexes", "A virtual relational interface that hides the underlying query structure", "A physical backup copy in every database engine"], "answer": 2, "explanation": "A view presents a query-defined interface that can abstract underlying table and join complexity.", "code": null, "tag": "Accenture-style Hard Practice", "difficulty": "Hard" }, { "q": "A table's primary key must uniquely identify each row and should not contain NULL. Which combination best matches standard relational expectations for a primary key?", "options": ["Uniqueness plus non-nullability", "Duplicate values plus nullable rows", "Only alphabetical characters", "Automatic encryption of every row"], "answer": 0, "explanation": "A primary key identifies rows uniquely and is not nullable under standard relational constraints.", "code": null, "tag": "Accenture-style Hard Practice", "difficulty": "Hard" }, { "q": "An analyst repeatedly runs a query that filters by `status` and `created_at` on a very large table. The optimizer can potentially benefit from an index aligned with the frequent predicate pattern. What should be evaluated before creating the index?", "options": ["Query selectivity, workload, column order, write cost and existing indexes", "Whether the UI uses dark mode", "Only the table name", "Whether the database has a primary key at all"], "answer": 0, "explanation": "Indexes improve some reads but add storage and write/maintenance cost; selectivity and query patterns determine whether an index is worthwhile.", "code": null, "tag": "Accenture-style Hard Practice", "difficulty": "Hard" }, { "q": "A transaction must ensure that two related updates cannot be interleaved in a way that exposes an invalid intermediate state to other transactions. Which concept is most directly relevant?", "options": ["HTML semantics", "Transaction isolation", "CPU cache associativity", "DNS TTL"], "answer": 1, "explanation": "Isolation controls how concurrent transactions interact and what intermediate effects are visible to one another.", "code": null, "tag": "Accenture-style Hard Practice", "difficulty": "Hard" }] }, { "id": "os", "name": "Operating Systems", "short": "OS", "questions": [{ "q": "A process requests a virtual memory page that is valid for its address space but is not currently loaded in physical RAM. The CPU traps into the operating system so the page can be located or brought in. What event has occurred?", "options": ["DNS cache miss", "Page fault", "Context-free grammar", "Deadlock by definition"], "answer": 1, "explanation": "A page fault occurs when a referenced virtual page is not currently resident in the required physical memory frame.", "code": null, "tag": "Accenture-style Hard Practice", "difficulty": "Hard" }, { "q": "A CPU scheduler repeatedly gives each ready process a small time slice before moving to the next process. The goal is to provide responsive sharing among interactive tasks rather than allowing one process to monopolize the CPU. Which algorithm matches this description?", "options": ["Static disk scheduling", "Round Robin", "First Come First Served without preemption", "Shortest Job First only"], "answer": 1, "explanation": "Round Robin uses time quanta and cycles through ready processes, supporting fair responsive CPU sharing.", "code": null, "tag": "Accenture-style Hard Practice", "difficulty": "Hard" }, { "q": "A system has four processes and four resource instances. Each process holds one resource and waits for another held by a different process, creating a circular dependency. Why can the system become permanently stuck without intervention?", "options": ["Paging guarantees progress", "A cache miss creates a permanent lock", "The scheduler automatically resolves every circular wait", "The circular wait condition contributes to deadlock"], "answer": 3, "explanation": "Circular wait is one of the necessary conditions for deadlock; without prevention, avoidance or recovery, progress can stop.", "code": null, "tag": "Accenture-style Hard Practice", "difficulty": "Hard" }, { "q": "A process moves from running to ready because its time quantum expires while it still has work to do. Which scheduling behavior explains the transition?", "options": ["Preemption by the scheduler", "Page replacement", "File allocation", "Permanent termination"], "answer": 0, "explanation": "In a preemptive scheduler, the OS can interrupt a running process and place it back in the ready queue.", "code": null, "tag": "Accenture-style Hard Practice", "difficulty": "Hard" }, { "q": "A system uses virtual memory and starts experiencing a very high rate of page faults, leaving little CPU time for useful application work. Which condition best describes this behavior?", "options": ["Spooling", "Deadlock prevention", "Thrashing", "Fragmentation-free execution"], "answer": 2, "explanation": "Thrashing occurs when excessive paging/page faults consume substantial system resources and reduce useful computation.", "code": null, "tag": "Accenture-style Hard Practice", "difficulty": "Hard" }, { "q": "Two processes share a variable and both perform a read-modify-write increment without synchronization. Some increments disappear even though each operation appears correct in isolation. What concurrency issue is most likely?", "options": ["Race condition", "Starvation caused by DNS", "Page fault", "Syntax error"], "answer": 0, "explanation": "A race condition occurs when the outcome depends on timing of unsynchronized concurrent accesses to shared state.", "code": null, "tag": "Accenture-style Hard Practice", "difficulty": "Hard" }, { "q": "A thread is blocked waiting for I/O and therefore cannot continue executing until the I/O completes. Which process/thread state most closely represents this situation?", "options": ["Waiting/blocked", "Running", "Ready with CPU ownership", "Terminated"], "answer": 0, "explanation": "A blocked or waiting state represents a task that cannot proceed until an external event such as I/O completion occurs.", "code": null, "tag": "Accenture-style Hard Practice", "difficulty": "Hard" }, { "q": "A process creates a child process. The parent and child initially share certain memory pages efficiently, but a write by one process requires a private copy of the affected page. Which mechanism explains this optimization?", "options": ["Round Robin", "Spooling", "Demand paging only without sharing", "Copy-on-write"], "answer": 3, "explanation": "Copy-on-write allows processes to share pages until a write requires a private copy, reducing unnecessary duplication.", "code": null, "tag": "Accenture-style Hard Practice", "difficulty": "Hard" }, { "q": "A system has many small free memory holes separated by allocated blocks. A large contiguous allocation fails even though the total free memory is sufficient. Which problem does this illustrate?", "options": ["Thrashing", "Deadlock", "External fragmentation", "Internal scheduling"], "answer": 2, "explanation": "External fragmentation occurs when free memory is split into separate holes that cannot satisfy a large contiguous request.", "code": null, "tag": "Accenture-style Hard Practice", "difficulty": "Hard" }, { "q": "A file descriptor refers to an open file, and multiple processes or threads may share references to the same underlying open file description depending on how it was created. Why is the descriptor best understood as an OS-managed handle rather than the file's contents themselves?", "options": ["It identifies an open resource maintained by the OS, with metadata/state associated with the open instance", "It is a network cable identifier", "It stores the entire file contents in the CPU register", "It is the same thing as a DNS record"], "answer": 0, "explanation": "A file descriptor is a process-level handle used to refer to an OS-managed open resource; the descriptor itself is not the file contents.", "code": null, "tag": "Accenture-style Hard Practice", "difficulty": "Hard" }, { "q": "A scheduler always chooses the process with the smallest estimated CPU burst. Short jobs finish quickly, but a long-running process can wait for a very long time if short jobs keep arriving. What issue can result?", "options": ["Internal fragmentation only", "Deadlock in every case", "Starvation", "Page fault"], "answer": 2, "explanation": "Starvation occurs when a task is indefinitely delayed because scheduling preference continually favors other tasks.", "code": null, "tag": "Accenture-style Hard Practice", "difficulty": "Hard" }, { "q": "A context switch occurs when the OS stops one running process/thread and resumes another. Which information generally must be preserved so the resumed execution can continue correctly?", "options": ["The entire hard disk every time", "Only the process name string", "Only the monitor resolution", "Execution context such as registers/program counter and relevant scheduling state"], "answer": 3, "explanation": "The OS saves and restores CPU execution context so the next task can resume from the correct state.", "code": null, "tag": "Accenture-style Hard Practice", "difficulty": "Hard" }, { "q": "A process requests more stack memory than the system can provide and repeatedly consumes stack space through unbounded recursion. Which failure is most directly associated with exhausting the call stack?", "options": ["Page-table normalization", "Stack overflow", "SQL deadlock", "DNS timeout"], "answer": 1, "explanation": "Unbounded recursion can exhaust the available call stack and cause a stack overflow.", "code": null, "tag": "Accenture-style Hard Practice", "difficulty": "Hard" }, { "q": "A system wants a process to wait until a child process finishes before continuing a particular parent operation. In Unix-like process models, which family of system-call behavior is commonly used for this synchronization?", "options": ["printf only", "wait/waitpid", "chmod only", "malloc only"], "answer": 1, "explanation": "wait and waitpid allow a parent to wait for child process state changes, commonly including termination.", "code": null, "tag": "Accenture-style Hard Practice", "difficulty": "Hard" }, { "q": "A memory allocator rounds a 13-byte request up to a 16-byte block because of alignment and allocator size classes. The unused bytes inside the allocated block are an example of what?", "options": ["Starvation", "Deadlock", "Internal fragmentation", "External fragmentation"], "answer": 2, "explanation": "Unused space inside an allocated block is internal fragmentation.", "code": null, "tag": "Accenture-style Hard Practice", "difficulty": "Hard" }] }, { "id": "dsa", "name": "DSA & Algorithms", "short": "DSA", "questions": [{ "q": "A service receives 2 million sorted customer IDs and must answer thousands of exact membership queries. The IDs are stored in a contiguous array and no frequent insertion is required. Which approach gives a strong asymptotic lookup guarantee while using the existing ordering?", "options": ["Randomly sample 10 elements", "Binary search", "Linear scan from index 0 for every query", "Bubble sort before every lookup"], "answer": 1, "explanation": "Binary search exploits sorted order and answers exact membership queries in O(log n) time.", "code": null, "tag": "Accenture-style Hard Practice", "difficulty": "Hard" }, { "q": "A binary search tree contains n keys but is unbalanced and has degenerated into a chain because keys were inserted in sorted order. What is the worst-case search complexity?", "options": ["O(n)", "O(n log n)", "O(1)", "O(log n)"], "answer": 0, "explanation": "A degenerate BST can have height n, making search O(n) in the worst case.", "code": null, "tag": "Accenture-style Hard Practice", "difficulty": "Hard" }, { "q": "A system repeatedly needs to process the smallest-priority job, and new jobs can be inserted while processing continues. Which data structure is designed to support efficient priority-based removal rather than simple insertion order?", "options": ["Heap/priority queue", "Plain unsorted linked list with no search policy", "Circular buffer only", "Stack"], "answer": 0, "explanation": "A heap-backed priority queue supports efficient insertion and removal of the highest- or lowest-priority element.", "code": null, "tag": "Accenture-style Hard Practice", "difficulty": "Hard" }, { "q": "A graph represents cities connected by roads, and every road has the same cost. The application needs the minimum number of roads from a source city to every reachable city. Which traversal is appropriate?", "options": ["Breadth-first search", "Binary search", "Depth-first search only", "Heap sort"], "answer": 0, "explanation": "BFS explores an unweighted graph level by level, giving shortest path length in number of edges.", "code": null, "tag": "Accenture-style Hard Practice", "difficulty": "Hard" }, { "q": "A weighted graph has non-negative edge weights. The application needs shortest paths from one source to all reachable vertices. Which classical algorithm is appropriate under the non-negative-weight assumption?", "options": ["Binary search", "Dijkstra's algorithm", "Kruskal's algorithm only for one-source distances", "DFS with no distance relaxation"], "answer": 1, "explanation": "Dijkstra's algorithm computes single-source shortest paths when edge weights are non-negative.", "code": null, "tag": "Accenture-style Hard Practice", "difficulty": "Hard" }, { "q": "A developer wants to sort a large array and needs guaranteed O(n log n) worst-case time, accepting additional memory for a predictable divide-and-conquer implementation. Which algorithm best fits?", "options": ["Selection sort", "Bubble sort", "Naive recursive permutation", "Merge sort"], "answer": 3, "explanation": "Merge sort has O(n log n) worst-case time and typically uses additional memory for merging.", "code": null, "tag": "Accenture-style Hard Practice", "difficulty": "Hard" }, { "q": "A quicksort implementation chooses a pivot poorly on already sorted data and repeatedly creates partitions of sizes 0 and n-1. What worst-case time complexity can result from this recursion pattern?", "options": ["O(n log n)", "O(n)", "O(n²)", "O(log n)"], "answer": 2, "explanation": "Highly unbalanced quicksort partitions lead to roughly n recursive levels and O(n) work per level, giving O(n²) worst-case time.", "code": null, "tag": "Accenture-style Hard Practice", "difficulty": "Hard" }, { "q": "A sliding-window algorithm processes an array with left and right pointers. Each pointer moves only forward and each element enters and leaves the window at most once. What is the overall time complexity?", "options": ["O(log n)", "O(n)", "O(2^n)", "O(n²)"], "answer": 1, "explanation": "Both pointers advance monotonically, so the total number of pointer movements is linear in n.", "code": null, "tag": "Accenture-style Hard Practice", "difficulty": "Hard" }, { "q": "A problem asks for the number of ways to reach step n when you can climb either one or two steps at a time. A direct recursive solution repeats the same subproblems many times. Which technique most directly reduces this repeated work?", "options": ["Depth-first search without caching", "Dynamic programming with memoization or tabulation", "Randomized pivot selection", "Binary search"], "answer": 1, "explanation": "The problem has overlapping subproblems; memoization or tabulation stores previously computed results and reduces repeated work.", "code": null, "tag": "Accenture-style Hard Practice", "difficulty": "Hard" }, { "q": "A set of tasks has precedence constraints such as A before C and B before C. The system needs an ordering that respects all dependencies, and the dependency graph is guaranteed to be a DAG. Which algorithmic approach is appropriate?", "options": ["Binary search", "Two-pointer reversal", "Topological sorting", "Heap sort only on task names"], "answer": 2, "explanation": "Topological sorting produces an ordering of vertices in a directed acyclic graph that respects all directed dependencies.", "code": null, "tag": "Accenture-style Hard Practice", "difficulty": "Hard" }, { "q": "A stack is implemented with an array and maintains a top index. Push and pop both modify only the top element rather than shifting the remaining elements. Assuming capacity is available, what are their typical time complexities?", "options": ["O(log n) each", "O(n²) each", "O(n) each", "O(1) each"], "answer": 3, "explanation": "Array-based stack push and pop operate at the top and therefore take constant time when no resizing is required.", "code": null, "tag": "Accenture-style Hard Practice", "difficulty": "Hard" }, { "q": "A queue is implemented using a linked list with pointers to both the front and rear. Enqueue inserts at the rear and dequeue removes from the front. Assuming the pointers are maintained correctly, what are the typical complexities?", "options": ["O(log n) for both", "O(n) enqueue and O(1) dequeue", "O(1) for both", "O(n²) for both"], "answer": 2, "explanation": "Rear and front pointers allow both operations to change a constant number of links.", "code": null, "tag": "Accenture-style Hard Practice", "difficulty": "Hard" }, { "q": "A hash table uses separate chaining. With a good hash function and a controlled load factor, what is the expected average-case complexity for a key lookup?", "options": ["O(log n) always", "O(n²)", "O(1)", "O(n log n)"], "answer": 2, "explanation": "With good hashing and controlled load factor, the expected average lookup is constant time, although the worst case can degrade.", "code": null, "tag": "Accenture-style Hard Practice", "difficulty": "Hard" }, { "q": "A minimum spanning tree is required for a connected weighted undirected graph, minimizing total edge weight without creating cycles. Which algorithm is a standard solution?", "options": ["Floyd-Warshall only for all-pairs paths", "BFS without weights", "Binary search", "Kruskal's or Prim's algorithm"], "answer": 3, "explanation": "Kruskal and Prim are standard minimum-spanning-tree algorithms for weighted undirected graphs.", "code": null, "tag": "Accenture-style Hard Practice", "difficulty": "Hard" }, { "q": "A recursive algorithm splits an input of size n into two subproblems of size n/2 and performs O(n) additional work to combine their results. Which recurrence and asymptotic complexity best describe it?", "options": ["T(n)=2T(n/2)+O(n), giving O(n log n)", "T(n)=T(n/2)+O(1), giving O(n²)", "T(n)=3T(n)+O(n), giving O(n)", "T(n)=T(n-1)+O(1), giving O(log n)"], "answer": 0, "explanation": "The recurrence matches merge-sort-style divide and conquer; each level performs linear work and there are logarithmically many levels.", "code": null, "tag": "Accenture-style Hard Practice", "difficulty": "Hard" }] }];
const TOTAL = BANK.reduce((n, s) => n + s.questions.length, 0);
let state = {
    section: 0,
    index: 0,
    answers: {},
    flags: {},
    theme: localStorage.getItem('cm_acc_theme') || 'light',
    started: Date.now(),
    submitted: false
};
/** Thin typed wrapper around document.querySelector. Matches the original
 *  code's assumption that the requested element always exists. */
function $(selector) {
    return document.querySelector(selector);
}
function key() {
    return state.section + "-" + state.index;
}
function section() {
    return BANK[state.section];
}
function q() {
    return section().questions[state.index];
}
function esc(s) {
    return String(s).replace(/[&<>"']/g, (m) => ({
        "&": "&amp;",
        "<": "&lt;",
        ">": "&gt;",
        '"': "&quot;",
        "'": "&#039;"
    }[m]));
}
function renderSections() {
    const currentComplete = section().questions.every((_, qi) => state.answers[state.section + "-" + qi] !== undefined);
    $("#sections").innerHTML = BANK.map((s, i) => {
        const done = s.questions.every((_, qi) => state.answers[i + "-" + qi] !== undefined);
        const locked = i > state.section + 1 || (i === state.section + 1 && !currentComplete);
        return `<button type="button" class="sectionbtn ${i === state.section ? "active" : ""} ${locked ? "locked" : ""}" data-section="${i}" aria-disabled="${locked}">
      <span class="sectionIcon">${["▦", "⌕", "⌘", "◈", "</>", "JS", "OOP", "DB", "OS", "DS"][i]}</span>
      <span class="sectionName">${s.name}</span>
      <span class="sectionCount">${done ? "✓ " : ""}${s.questions.length} Q</span>
      <i class="activeLine"></i>
    </button>`;
    }).join("");
    document.querySelectorAll(".sectionbtn").forEach((btn) => {
        btn.addEventListener("click", function (e) {
            e.preventDefault();
            e.stopPropagation();
            const target = Number(this.dataset.section);
            if (target === state.section || target < state.section) {
                switchSection(target);
                return;
            }
            if (target === state.section + 1) {
                const complete = section().questions.every((_, qi) => state.answers[state.section + "-" + qi] !== undefined);
                if (complete) {
                    switchSection(target);
                }
                else {
                    const keyBox = $("#sectionKey");
                    if (keyBox)
                        keyBox.classList.remove("show");
                    alert("Please complete all 15 questions in the current section first.");
                }
            }
        });
    });
}
function renderNav() {
    $("#navgrid").innerHTML = section().questions.map((x, i) => {
        const k = state.section + "-" + i;
        return `<button type="button" class="qnav ${i === state.index ? "current" : ""} ${state.answers[k] !== undefined ? "answered" : ""}" data-q="${i}">${i + 1}</button>`;
    }).join("");
    document.querySelectorAll(".qnav").forEach((btn) => {
        btn.addEventListener("click", function (e) {
            e.preventDefault();
            e.stopPropagation();
            go(Number(this.dataset.q));
        });
    });
}
function render() {
    const x = q();
    const k = key();
    const chosen = state.answers[k];
    $("#sectionLabel").querySelector("b").textContent = section().name;
    $("#qnum").textContent = `Question ${state.index + 1} of ${section().questions.length}`;
    $("#qtitle").textContent = x.q;
    $("#tag").textContent = x.difficulty || "Medium";
    $("#tag").className = "badge " + (x.difficulty === "Hard" ? "hard" : x.difficulty === "Medium" ? "medium" : "easy");
    $("#code").style.display = x.code ? "block" : "none";
    $("#code").textContent = x.code || "";
    $("#options").innerHTML = x.options.map((o, i) => {
        let cls = "opt";
        if (chosen !== undefined) {
            if (i === x.answer)
                cls += " correct";
            if (i === chosen && chosen !== x.answer)
                cls += " wrong";
        }
        return `<button class="${cls}" ${chosen !== undefined ? "disabled" : ""} onclick="answer(${i})"><span class="letter">${String.fromCharCode(65 + i)}.</span><span>${esc(o)}</span></button>`;
    }).join("");
    $("#explain").style.display = chosen === undefined ? "none" : "block";
    $("#explainText").textContent = x.explanation;
    $("#flagBtn").textContent = state.flags[k] ? "★ Marked" : "☆ Mark for review";
    $("#prevBtn").disabled = state.index === 0;
    if (state.index === section().questions.length - 1) {
        const complete = section().questions.every((_, i) => state.answers[state.section + "-" + i] !== undefined);
        $("#nextBtn").textContent = state.section === BANK.length - 1
            ? (complete ? "View Final Result" : "Finish Section")
            : (complete ? "Next Section →" : "Finish Section");
    }
    else {
        $("#nextBtn").textContent = "Next →";
    }
    renderNav();
    updateScore();
    renderSectionKey();
}
function answer(i) {
    const k = key();
    if (state.answers[k] !== undefined)
        return;
    state.answers[k] = i;
    render();
    if (state.index === section().questions.length - 1) {
        const complete = section().questions.every((_, qi) => state.answers[state.section + "-" + qi] !== undefined);
        if (complete) {
            renderSectionKey();
            renderSections();
            const keyBox = $("#sectionKey");
            if (keyBox) {
                keyBox.classList.add("show");
                keyBox.style.display = "block";
                setTimeout(() => keyBox.scrollIntoView({ behavior: "smooth", block: "start" }), 80);
            }
        }
    }
}
function go(i) {
    state.index = i;
    render();
    scrollTo({ top: 0, behavior: "smooth" });
}
function next() {
    if (state.index < section().questions.length - 1) {
        state.index++;
        render();
        window.scrollTo({ top: 0, behavior: "smooth" });
        return;
    }
    const complete = section().questions.every((_, i) => state.answers[state.section + "-" + i] !== undefined);
    if (!complete)
        return;
    renderSectionKey();
    const keyBox = $("#sectionKey");
    if (keyBox) {
        keyBox.classList.add("show");
        keyBox.scrollIntoView({ behavior: "smooth", block: "start" });
    }
}
function continueFromKey() {
    const complete = section().questions.every((_, i) => state.answers[state.section + "-" + i] !== undefined);
    if (!complete)
        return;
    if (state.section < BANK.length - 1) {
        const nextSection = state.section + 1;
        state.section = nextSection;
        state.index = 0;
        const keyBox = $("#sectionKey");
        if (keyBox) {
            keyBox.classList.remove("show");
            keyBox.style.display = "none";
        }
        renderSections();
        render();
        window.scrollTo({ top: 0, behavior: "smooth" });
        return;
    }
    openResults();
}
function prev() {
    if (state.index > 0) {
        state.index--;
        render();
        scrollTo({ top: 0, behavior: "smooth" });
    }
}
function flag() {
    state.flags[key()] = !state.flags[key()];
    render();
}
function switchSection(i) {
    if (!Number.isInteger(i) || i < 0 || i >= BANK.length)
        return;
    if (i > state.section) {
        const complete = section().questions.every((_, qi) => state.answers[state.section + "-" + qi] !== undefined);
        if (!complete || i !== state.section + 1)
            return;
    }
    state.section = i;
    state.index = 0;
    const keyBox = $("#sectionKey");
    if (keyBox) {
        keyBox.classList.remove("show");
        keyBox.style.display = "none";
    }
    const main = document.querySelector(".main");
    if (main) {
        main.classList.remove("sectionPulse");
        void main.offsetWidth;
        main.classList.add("sectionPulse");
    }
    renderSections();
    render();
    window.scrollTo({ top: 0, behavior: "smooth" });
}
function renderSectionKey() {
    const completed = section().questions.every((_, i) => state.answers[state.section + "-" + i] !== undefined);
    const box = $("#sectionKey");
    if (!completed) {
        box.classList.remove("show");
        box.style.display = "none";
        return;
    }
    $("#keySectionName").textContent = section().name + " — Answer Key";
    $("#keyGrid").innerHTML = section().questions.map((x, i) => {
        const chosen = state.answers[state.section + "-" + i];
        const ok = chosen === x.answer;
        return `<div class="keyItem ${ok ? "ok" : "bad"}">
      <b>Q${i + 1}</b>
      <span>Correct: ${String.fromCharCode(65 + x.answer)}</span>
      <em>${ok ? "✓" : "✕"}</em>
    </div>`;
    }).join("");
    $("#nextSectionBtn").textContent = state.section === BANK.length - 1 ? "View Final Result →" : "Next Section →";
    box.classList.add("show");
    box.style.display = "block";
}
function updateScore() {
    let c = 0;
    Object.entries(state.answers).forEach(([k, v]) => {
        const [si, qi] = k.split("-").map(Number);
        if (BANK[si].questions[qi].answer === v)
            c++;
    });
    const attempted = Object.keys(state.answers).length;
    $("#score").textContent = `Score: ${c} / ${TOTAL}`;
    $("#answered").textContent = `${attempted} answered`;
    $("#progress").style.width = `${attempted / TOTAL * 100}%`;
}
function openResults() {
    state.submitted = true;
    let c = 0;
    Object.entries(state.answers).forEach(([k, v]) => {
        const [si, qi] = k.split("-").map(Number);
        if (BANK[si].questions[qi].answer === v)
            c++;
    });
    const a = Object.keys(state.answers).length;
    const p = a ? Math.round(c / a * 100) : 0;
    $("#finalScore").textContent = `${c}/${a}`;
    $("#accuracy").textContent = p + "%";
    $("#attempted").textContent = String(a);
    $("#remaining").textContent = String(TOTAL - a);
    $("#flagged").textContent = String(Object.values(state.flags).filter(Boolean).length);
    $("#result").classList.add("show");
    $("#result").scrollIntoView({ behavior: "smooth", block: "start" });
}
function resetQuiz() {
    var _a;
    if (!confirm("Reset all answers, flags and score?"))
        return;
    state = {
        section: 0,
        index: 0,
        answers: {},
        flags: {},
        theme: state.theme,
        started: Date.now(),
        submitted: false
    };
    $("#result").classList.remove("show");
    (_a = $("#sectionKey")) === null || _a === void 0 ? void 0 : _a.classList.remove("show");
    render();
    startTimer();
    scrollTo({ top: 0 });
}
let timerHandle = null;
function startTimer() {
    const limit = 45 * 60;
    const tick = () => {
        const left = Math.max(0, limit - Math.floor((Date.now() - state.started) / 1000));
        const m = String(Math.floor(left / 60)).padStart(2, "0");
        const s = String(left % 60).padStart(2, "0");
        const el = $("#timer");
        if (el) {
            el.textContent = m + ":" + s;
            el.classList.toggle("warn", left <= 600 && left > 180);
            el.classList.toggle("danger", left <= 180);
        }
        if (left === 0 && !state.submitted) {
            if (timerHandle)
                clearInterval(timerHandle);
            openResults();
        }
    };
    tick();
    if (timerHandle)
        clearInterval(timerHandle);
    timerHandle = setInterval(tick, 1000);
}
document.addEventListener("keydown", (e) => {
    var _a;
    const activeTag = (_a = document.activeElement) === null || _a === void 0 ? void 0 : _a.tagName;
    if (activeTag && ["INPUT", "TEXTAREA", "SELECT"].includes(activeTag))
        return;
    if (e.key === "ArrowRight")
        next();
    if (e.key === "ArrowLeft")
        prev();
});
document.body.classList.toggle("dark", state.theme === "dark");
document.addEventListener("DOMContentLoaded", () => {
    renderSections();
    render();
    startTimer();
});
